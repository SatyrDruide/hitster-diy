<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Music Timeline</title>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">

  <link rel="manifest" href="manifest.json">
</head>

<body style="background:#111;color:#fff;text-align:center;font-family:sans-serif;padding-top:40px">

  <h2>🎵 Song läuft</h2>

  <button onclick="playSong()" style="font-size:20px;padding:15px;margin:10px">
    ▶ Abspielen
  </button>

  <br>

  <button onclick="reveal()" style="font-size:18px;padding:10px;margin-top:20px">
    📅 Aufdecken
  </button>

  <script src="songs.js"></script>
  <script>
    const params = new URLSearchParams(window.location.search);
    const id = params.get("id");
    const song = SONGS[id];
    const audio = new Audio(song.preview);

    function playSong() {
      audio.play();
    }

    function reveal() {
      alert(`${song.title} (${song.year})`);
    }
  </script>

</body>
</html>
